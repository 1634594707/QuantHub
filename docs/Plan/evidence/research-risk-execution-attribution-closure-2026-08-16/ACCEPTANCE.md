# 2026-08-16 闭环发布验收

结论：通过。

## 自动化门禁

- 后端：`uv run --frozen python -m pytest -q`，最终格式化后复跑 `392 passed in 46.76s`。
- 后端静态检查：相关改动 Ruff 通过。
- 前端：`npm.cmd run test -- --run`，`38` 个测试文件、`152` 项测试通过。
- 前端类型：`npm run typecheck` 通过。
- 前端构建：`npm run build` 通过，Vite 完成 `1805` 个模块转换。

## 浏览器验收

- URL：`http://127.0.0.1:5173`
- 浏览器：Chromium headless
- 视口：`390x844`、`768x1024`、`1440x900`
- 场景：模拟风控阻断；研究冲突与历史结构化差异；账本因子版本、研究筛选、未知归因和收益守恒。
- 结果：9/9 场景通过；无横向溢出、文字裁切、控制台错误、失败请求或 HTTP 错误响应。

机器可读结果见 `browser-acceptance-report.json`。截图与报告使用确定性 API 夹具，页面代码、CSS、路由和交互为真实构建；未向业务数据库写入研究、订单或账本数据。

## 覆盖结论

- 行情或账户证据缺失时，模拟订单失败关闭。
- 跨模块方向冲突显示为阻断状态，隐藏执行性价位并禁用模拟入口。
- 历史比较展示方向、执行资格、指标与价位的结构化变化。
- 同因子不同版本独立归因，维度筛选和稳定回链可用。
- 未知归因单独展示，分组净收益与账本净收益守恒。
- A 股、美股和 OKX 完整成本档案均有契约测试。
