[build-system]
requires = ["hatchling>=1.21"]
build-backend = "hatchling.build"

[project]
name = "quanthub-morning-brief"
version = "0.4.0"
description = "A股晨会简报自动生成"
requires-python = ">=3.11,<3.13"
dependencies = ["quanthub-core", "openai>=1.40"]

[tool.hatch.build.targets.wheel]
packages = ["."]
