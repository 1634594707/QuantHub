[build-system]
requires = ["hatchling>=1.21"]
build-backend = "hatchling.build"

[project]
name = "quanthub-perks-monitor"
version = "0.1.0"
description = "A股股东回馈羊毛监控"
requires-python = ">=3.11,<3.13"
dependencies = ["quanthub-core"]

[tool.hatch.build.targets.wheel]
packages = ["."]
