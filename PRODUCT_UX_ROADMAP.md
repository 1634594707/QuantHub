# QuantHub 产品与用户体验路线图

> 建立日期：2026-07-28
>
> 状态规则：`[x]` 表示当前工作树已实现并完成对应验证；`[ ]` 表示尚未达到验收条件。
>
> 证据规则：每项完成时必须同时更新源码、自动化验证和本文件中的验收记录。接口、字段、路径和状态名称全部以当前仓库中的精确定义为准。
>
> 文档收口规则：本文件是后续唯一产品路线图。只有本文件全部项目完成后，才删除 `FUNCTIONAL_ROADMAP.md`、`USER_FUNCTIONAL_ROADMAP.md` 和 `BEGINNER_STOCK_EVALUATION_ROADMAP.md`。

## 已确认的产品基线

- [x] 五个一级工作区已经建立：驾驶舱、研究、策略、执行、运营。
- [x] 研究运行支持记录、证据、收藏、备注、校验、对比和 JSON 导出。
- [x] 信号支持审核状态、审核备注、执行预览和转模拟订单。
- [x] 模拟成交支持账本同步状态和同步失败重试。
- [x] 账本支持持仓、成交、现金、绩效、敞口、归因、基准、更正和决策时间线。
- [x] 策略实验室支持定义、版本、实验、运行、差异比较和归档。
- [x] 自动化中心支持启停、Cron 编辑、运行、历史、重试、告警确认和审计。
- [x] 运营工作区已包含股票与市场、故障状态、成员与访问、系统配置和备份管理。

## P0 高频流程与状态真实性

### 统一导航和上下文

- [x] 用统一路由元数据生成页面标识、顶栏名称、侧栏、移动端激活状态和全局检索页面入口。
- [x] `/incidents` 显示“故障状态”，不再回退为“总览”。
- [x] 移动端在 `/incidents` 和 `/governance` 正确激活“更多”。
- [x] 全局检索包含“成员与访问”。
- [x] 移动端研究入口进入最近一次研究上下文；没有历史上下文时进入 `/evaluate`。
- [x] 最近研究股票、市场、周期和视图在浏览器重启后恢复。

### 连接、鉴权和数据状态

- [x] `useApi` 支持成功后的轮询，并在页面隐藏时暂停轮询。
- [x] 顶栏信号数量、策略数量和健康状态按明确周期刷新。
- [x] 网关连接、访问权限、业务数据源和降级状态分别展示。
- [x] 页面展示最近成功更新时间；数据超过业务允许时效后显示过期状态。
- [x] 401 显示“需要访问令牌”，403 显示缺失的权限，不再显示“后端未连接”。
- [x] `/health` 返回运行进程启动时间、版本和源码构建标识。
- [x] 系统配置展示 A 股数据源、新闻引擎、模型和可选依赖的实际可用状态。

### 账户口径

- [x] 驾驶舱明确区分研究组合、模拟账户和账本账户。
- [x] 驾驶舱账户指标由用户选择的数据口径驱动，标题、金额和持仓数保持一致。
- [x] 研究组合不得使用“账户净值”描述；模拟账户不得使用真实账户描述。
- [x] 当前口径写入 URL 或本地偏好，并在刷新后恢复。

### 高频记录访问

- [x] 研究运行列表增加服务端游标分页和总数。
- [x] 信号列表增加服务端游标分页和总数。
- [x] 模拟订单列表增加服务端游标分页和总数。
- [x] 分析任务、自动化运行、故障、审计、账本成交和现金列表增加服务端游标分页和总数。
- [x] 页面支持继续加载，并保留当前筛选、选中记录和滚动位置。
- [x] 全局检索不受各业务页面首批加载数量限制。

## P0 统一股票评估

### 一次提交

- [x] 从 `/evaluate` 一次提交创建统一股票评估运行。
- [x] 默认模块精确定义为行情、新闻、价格行为和多模型判断。
- [x] 页面显示准备数据、行情分析、新闻分析、价格行为、多模型汇总和报告生成进度。
- [x] 单个模块失败时保留其他模块结果，并把运行标记为部分完成。
- [x] 超时后保留已完成结果和精确失败原因。
- [x] 支持取消、重新评估和从失败模块继续。
- [x] 相同股票、市场和周期短时间重复提交时显示已有运行并允许复用。

### 可读报告

- [x] 首屏显示股票名称、代码、最新价格、价格时间、数据来源和适用周期。
- [x] 结论使用偏强、中性、偏弱或数据不足，不使用伪精确综合分数。
- [x] 报告先展示结论、三条以内主要依据、主要风险、失效条件和下一步观察项。
- [x] 多模块意见冲突时明确展示分歧，不把缺失模块计入综合结论。
- [x] 每条结论可以展开查看数据来源、快照时间、模型版本和原始依据。
- [x] 提供加入自选、设置提醒、与上次评估对比和进入模拟交易。
- [x] 提供面向阅读的 HTML 和 PDF 导出；JSON 保留为专业数据导出。
- [x] 报告固定显示“辅助研究，不构成投资建议”。

## P1 基本面与估值

- [ ] 确定财务数据源、许可证、调用限制和更新频率，并写入配置。
- [ ] 接入收入、利润、经营现金流和资产负债数据。
- [ ] 接入 PE、PB、股息率、市值、ROE、毛利率、净利率和负债率。
- [ ] 保存数据来源、报告期、更新时间和币种。
- [ ] 展示营收利润趋势、盈利质量、现金流匹配和资产负债风险。
- [ ] 展示行业分类、行业区间和可比公司，不使用单一分数掩盖维度冲突。
- [ ] 区分“公司质量”和“当前价格是否合理”。
- [ ] 基本面数据过期时降低结论可靠度并显示具体报告期。

## P1 提醒中心

- [x] 新增价格、涨跌幅、波动、信号、评估结论变化和风险失效提醒。
- [x] 提醒规则支持启停、阈值、频率、静默时段和到期时间。
- [x] 提供触发历史、确认状态和关联股票或研究运行。
- [x] 系统配置支持通知渠道配置、脱敏展示和发送测试。
- [x] 驾驶舱展示待处理提醒，并能直接进入关联记录。

## P1 治理与安全操作

- [x] 成员支持停用和恢复，停用后已有令牌立即失效。
- [x] 令牌撤销使用二次确认并展示影响对象和最近使用时间。
- [x] 成员与访问页面根据 `deployment_mode` 展示本机、局域网或 PostgreSQL 部署状态。
- [x] 当前身份、角色、权限和会话错误始终来自 `/auth/session`。
- [x] 所有写操作继续写入统一审计日志。

## P2 使用效率与展示

- [x] 驾驶舱模块支持按用户调整顺序和隐藏低频模块。
- [x] 股票评估入口优先展示最近搜索和自选股票。
- [x] 空白安装提供独立只读示例工作区，不混入用户数据。
- [ ] 清理默认数据库中的 `E2E-*`、`api_test` 和 `playwright_mobile` 数据。
- [x] 示例数据支持独立重置，不影响用户数据。
- [x] 新手模式只显示驾驶舱、股票评估、自选、模拟执行和设置。
- [x] 高级模式保留研究、策略、信号、账本和运营工作区。
- [x] 提供 Windows 一键启动与环境检查，失败时显示日志位置和处理动作。

## 自动化验收与发布门禁

- [x] 恢复前端组件测试命令并接入 CI。
- [x] 恢复 FastAPI 领域测试并接入 CI。
- [x] 增加统一股票评估的成功、部分成功、取消、超时和恢复测试。
- [x] 增加信号审核、模拟成交和账本同步闭环测试。
- [x] 增加移动端导航、记录展开和核心操作测试。
- [x] 增加远程鉴权的 401、403、令牌撤销和成员停用测试。
- [x] 增加分页稳定性测试，确保翻页期间新增记录不会造成重复或遗漏。
- [x] `npm run typecheck` 通过。
- [x] `npm run build` 通过。
- [x] FastAPI 应用导入、健康检查和数据库迁移检查通过。
- [x] 390x844、768x1024、1024x768 和 1440x900 视觉验收通过。

## 验证记录

- [x] 2026-07-28：在 `web` 目录执行 `npm run typecheck`，TypeScript 项目引用检查通过。
- [x] 2026-07-28：在 `web` 目录执行 `npm run build`，Vite 生产构建完成，共转换 188 个模块。
- [x] 2026-07-28：执行 `uv run python -m unittest discover -s tests -p "test_*.py" -v`，17 项领域测试通过。
- [x] 2026-07-28：FastAPI 应用成功导入，`GET /health` 返回 `status=ok`、`version=0.2.0` 和非空 `build_id`。
- [x] 2026-07-28：分别用已有 `0001` 临时库和全新临时库执行 `alembic upgrade head`，两条路径均到达 `0002 (head)`；临时数据库已删除。
- [x] 2026-07-28：提醒测试覆盖价格触发、停用、到期、静默时段、事件确认，以及评估结论首次建立基线和后续变化触发。
- [x] 2026-07-28：治理测试覆盖远程 401、403、成员停用后令牌立即失效，以及恢复成员不会恢复旧令牌。
- [x] 2026-07-28：通知配置测试确认企业微信、Webhook 和 Telegram 的已配置值只返回掩码，不返回原始明文。
- [x] 2026-07-28：在 `web` 目录执行 `npm test`，5 个测试文件中的 12 项测试通过，覆盖工作区模式、移动端导航、记录展开、核心操作、提醒关联跳转和通知开关。
- [x] 2026-07-28：统一股票评估测试覆盖成功、部分成功、失败、取消、超时和从失败模块恢复；信号测试覆盖接受、转模拟订单、成交和账本同步。
- [x] 2026-07-28：游标分页测试确认分析任务翻页期间插入新记录不会造成重复或遗漏。
- [x] 2026-07-28：`tools/start-quanthub.ps1 -SkipSync` 完成环境、端口、健康和 `build_id` 检查，启动 `http://127.0.0.1:5173`，日志位置为 `logs/launcher/`。
- [x] 2026-07-28：用 Chromium 检查 `/`、`/evaluate`、`/example`、`/alerts` 和 `/config` 在 390x844、768x1024、1024x768、1440x900 下的首屏；20 个页面均无横向溢出、可见操作文字裁切或浏览器错误。
- [x] 2026-07-28：浏览器交互确认示例依据可展开且重置后收起、驾驶舱排序写入 `quanthub.overview.modules.v1`、新手模式显示五个移动入口、通知密码输入框不回填明文、`/#watchlist` 锚点存在。

## 文档收口

- [ ] 本文件中的所有项目均已完成并保留验证记录。
- [x] README 的功能、安装、启动和部署说明与当前源码一致。
- [ ] 删除 `FUNCTIONAL_ROADMAP.md`。
- [ ] 删除 `USER_FUNCTIONAL_ROADMAP.md`。
- [ ] 删除 `BEGINNER_STOCK_EVALUATION_ROADMAP.md`。
