# QuantHub AI 因子发现与交易验证路线图

> 更新日期：2026-08-01
>
> 前置基础：[FACTOR_RESEARCH_ROADMAP.md](FACTOR_RESEARCH_ROADMAP.md) 已完成因子复验、持久化、成本、执行约束和研究治理能力。
>
> 本路线图目标：把固定的 14 个技术因子筛选器升级为可扩展、可审计、能够借助 AI 生成研究假设，但由确定性统计程序决定结论的因子发现系统。
>
> 状态说明：所有 `[ ]` 均为尚未完成的任务；只有代码、测试、真实数据运行和证据留存全部通过后才能改为 `[x]`。

### 当前证据快照

- 真实 A 股基线：`demo_artifacts/a_share_300_factor_baseline.json`，300 标的、516 个共同 ordinal session、14 个因子，SHA256 `c08cf40b23f3d066b42de178343382cd59ef186cc3a3edae144b3032b34e4e38`。
- 预注册实验：`demo_artifacts/a_share_preregistered_experiments_01_07.json`，实验 1/2/3/4/5/7 共 6 个，1 个通过、5 个失败，SHA256 `2ec29d9b6ba70e111c844e9204ae5a8c05defb0953c0dbac491be08a39775c9b`。
- 最新 `600519` 运行：`6ba1a0b8e1174b53a92c7b23c2fdb8db`，`usable_factors=0`、`multifactor_constructed=false`。
- 证据边界：现有 session 不是可声明的自然日期；历史退市/ST/指数成员、时点行业/市值/Beta 暴露和人工新闻标注仍缺失。所有模拟审计均固定 `live_trading_enabled=false`。

## 一、核心原则与状态边界

- [x] 将因子生命周期统一为 `draft`、`exploratory`、`research_passed`、`trading_validated`、`degraded`、`retired`，并为每次状态变化保存规则和证据。
- [x] 将“训练期被选中”改名为“探索候选”，禁止它直接表示统计通过或交易可用。
- [x] `research_passed` 只由锁定的样本外统计门禁产生，AI、人工备注和训练期排名均不得修改该状态。
- [x] `trading_validated` 必须额外通过目标市场的成本、容量、执行约束和组合增量价值验证。
- [x] 将“四个市场全部通过”改为可配置规则：目标市场通过是强制项，跨市场迁移只作为可选的稳健性证据。
- [x] AI 不读取最终锁定确认集的收益标签，不直接生成买卖指令，不运行任意 Python，不修改程序统计结论。

### 状态边界验收标准

- [x] 当一个研究批次没有 `research_passed` 因子时，系统显示“未发现合格因子”，且不能创建可交易策略实验。
- [x] 任一因子状态都能追溯到公式版本、数据快照、试验次数、验证窗口、成本档案和门禁版本。
- [x] 同一因子可以在 A 股通过、在加密资产失败，不因跨市场失败而覆盖目标市场结论。

## 二、P0：修正会改变研究结论的口径

- [x] 修正横截面 `horizon > 1` 时把重叠未来收益按日连续复利的问题。
- [x] 提供两种无重叠实现：每 `horizon` 日调仓，以及按调仓日拆分为 `horizon` 个持仓批次的 cohort portfolio。
- [x] 交易成本按真实调仓批次和实际换手扣除，不再对重叠的未来收益直接逐日扣费。
- [x] 横截面 IC 显著性使用 HAC/Newey-West 或 block bootstrap，最大滞后至少覆盖 `horizon - 1`。
- [x] 横截面同批和跨批候选统一纳入多重检验，不只校正当前页面展示的因子。
- [x] 将 A 股结果拆分为“多头最高分组相对基准”“多头加指数对冲”和“理论多空”，禁止把不可执行的底部做空默认标记为可交易。
- [x] 没有合格因子时，移除当前按训练 IC 回退进入多因子组合的逻辑；如需保留，必须标记为仅探索且不输出交易状态。
- [x] 合并完全等价或仅差常数倍的公式族，例如 `mean_reversion` 与 `bollinger_reversal`，避免重复假设影响多重检验和排名。
- [x] 将默认横截面门槛从“20 个日期、3 个标的”升级为按市场配置的有效样本门槛，并展示 `effective_dates`。
- [x] 为收益重叠、成本重复扣除、不可做空、候选回退和等价因子增加回归测试。

### P0 验收标准

- [x] 使用人工构造的 5 日收益样例时，cohort portfolio 与独立逐日持仓账本结果一致。
- [x] `horizon=1` 与现有非重叠结果保持兼容，`horizon=5` 不再产生重复复利。
- [x] 最新 `600519` 数据没有合格因子时，多因子结果明确显示“未构建”，不会混入两个 `reject` 因子。
- [x] A 股研究报告不会把理论空头收益计入默认可执行收益。

## 三、P0：建立真实的时点横截面研究底座

- [x] 建立第一个真实 A 股研究股票池，初始目标不少于 300 个标的、5 年历史和 500 个有效交易日。
- [ ] 股票池按日期保存成分，纳入退市、暂停上市、ST、停牌、上市不足期和历史指数调入调出信息。
- [x] 记录每个字段的 `event_time`、`available_time`、来源、抓取时间、复权方式和修订版本。
- [ ] 市值、行业、Beta 和基本面暴露使用对应日期可获得的数据，禁止用当前静态值回填历史。
- [x] 对行情、股票池和暴露数据执行覆盖率、重复、缺失、异常跳变和时间顺序检查。
- [x] 建立 A 股可交易性过滤：涨跌停、停牌、ST、上市天数、成交额、整手和 T+1。
- [x] 将每次横截面运行锁定到股票池快照、行情快照和暴露快照的哈希。
- [x] 用现有 14 个因子完成第一次真实横截面基线运行，并保存失败标的和缺失原因。
- [x] 为美股、加密资产和 MT5 分别定义独立股票池与执行语义，不复制 A 股门槛。

### 数据底座验收标准

- [x] 任意历史日期都能恢复当日真实可选股票集合，不能只看到今天仍存在的标的。
- [x] 任意基本面或事件字段都不能早于其 `available_time` 进入因子计算。
- [x] 第一次 A 股基线运行覆盖率、缺失率、中性化失败率和不可交易比例均可解释并可回链。

## 四、P1：因子注册表与安全 DSL

- [x] 新增不可变 `FactorDefinition` 注册表，保存因子 ID、名称、市场、输入字段、AST、参数、方向假设、持有期、可用延迟和经济逻辑。
- [x] 每个公式使用规范化 AST 哈希作为身份，参数或算子变化必须产生新版本。
- [x] DSL 首批只允许白名单算子：四则运算、比较、条件、滞后、差分、滚动统计、排名、截尾、标准化和行业中性化。
- [x] DSL 明确禁止负数滞后、未来窗口、全样本标准化和任何未声明外部函数。
- [x] 增加单位与形状检查，阻止价格直接与成交量相加等无意义表达式。
- [x] 增加复杂度预算，包括最大 AST 深度、最大算子数、最大参数组合数和最小数据覆盖率。
- [x] 建立公式族概念，将同一经济假设的参数变体计为同一试验族。
- [x] 对候选执行完全重复、单调等价、常数倍等价和高相关冗余检测。
- [x] 将现有 14 个技术因子迁移到注册表和 DSL，同时保持历史版本可恢复。
- [x] AlphaGPT 和 AlphaMaster 的 token 公式通过适配层映射到统一因子定义，不再形成独立的不可审计搜索路径。

### DSL 验收标准

- [x] 任一 AI 或人工候选必须先通过语法、因果、单位、复杂度和数据覆盖检查，之后才能进入计算队列。
- [x] 修改参数、公式、数据可用延迟或预处理方式后，因子版本和哈希必然变化。
- [x] `mean_reversion` 与仅差常数倍的公式会归入同一等价族。

## 五、P1：确定性因子发现与验证流水线

- [x] 将数据拆分为发现集、滚动验证集和最终锁定确认集，确认集只允许在预注册实验完成后开启一次。
- [x] 使用 purged walk-forward 和 embargo，隔离期覆盖标签持有期及数据发布时间延迟。
- [x] 支持按因子族进行有限参数网格搜索，但把全部参数尝试计入试验次数。
- [x] 增加参数平台测试，只有相邻参数区间均有效时才认为结果稳健。
- [x] 增加时间稳定性测试：年度、牛熊、趋势、震荡、高低波动和流动性状态。
- [x] 增加横截面稳定性测试：行业、市值分组、流动性分组和上市年龄分组。
- [x] 增加标签稳定性测试：未来 1、3、5、10、20 日残差收益和风险调整收益。
- [x] 增加安慰剂测试：标签打乱、时间平移、随机因子和伪事件日期。
- [x] 增加数据扰动测试：缺失、价格微扰、成本上浮、延迟成交和容量收缩。
- [x] 使用市场和行业中性后的残差收益作为主要横截面标签，原始收益作为辅助指标。
- [x] 因子排序采用 Pareto 前沿，不用单一综合分掩盖 IC、换手、容量、稳定性和相关性的取舍。
- [x] 保存所有失败候选及失败原因，禁止只保存最终胜出者。

### 发现流水线验收标准

- [x] 研究人员无法通过反复查看确认集结果继续调参而不留下新的试验记录。
- [x] 只有在多数窗口、主要分组和参数邻域均保持方向一致的因子才能进入 `research_passed`。
- [x] 标签打乱后，系统发现合格因子的比例符合设定的假阳性上限。

## 六、P1：全局试验账本与数据窥探控制

- [x] 新增 `FactorExperiment` 账本，记录假设、候选来源、父因子、公式族、数据范围、参数、模型、提示词、运行状态和结果。
- [x] 每一次人工修改、AI 生成、参数搜索和失败重试都生成不可变试验记录。
- [x] Benjamini-Hochberg 校正覆盖同一研究计划内的全部历史候选，而非单次 API 请求。
- [x] 增加 Deflated Sharpe Ratio，修正非正态收益和多次尝试造成的夏普膨胀。
- [x] 对大规模搜索批次增加 White Reality Check、SPA 或 block bootstrap 基准比较。
- [x] 在报告中同时展示原始 p 值、批次校正值、全局校正值、有效样本量和累计尝试次数。
- [x] 预注册每个实验的主指标、次指标、通过门槛、数据范围和最大候选预算。
- [x] 为研究计划设置候选数、计算量、LLM token 和确认集开启次数预算。

### 试验治理验收标准

- [x] 删除失败结果或拆分请求不能降低一个研究计划的累计试验次数。
- [x] AI 批量生成 100 个候选时，统计门禁按 100 个及其参数变体进行校正。
- [x] 历史研究可以完整回答“这个结论之前尝试过多少个公式和参数”。

## 七、P1：因子组合与真实增量价值

- [x] 计算候选与现有因子的 Pearson、Spearman、尾部和分市场状态相关性。
- [x] 对候选执行相对已部署因子的正交化，报告原始 IC 和增量 IC。
- [x] 使用时间序列外层验证组合权重，禁止根据最终确认集选择权重。
- [x] 建立简单基准：等权、IC 权重、风险平价和带正则约束的线性组合。
- [x] LightGBM、CatBoost 或其他非线性模型只能作为对照组，并使用嵌套时间序列验证。
- [x] 报告组合相对单因子的收益增量、换手增量、容量变化、回撤变化和尾部风险。
- [x] 将单因子有效但组合无增量价值的结果标记为“研究有效、组合不采用”。
- [x] 按目标市场配置权重、行业偏离、单股上限、成交额参与率和换手预算。

### 组合验收标准

- [x] 新因子必须在扣除增量成本后改善至少一个预注册组合目标，且不能突破风险硬约束。
- [x] 高度相关的因子不能仅因名称或公式不同重复获得权重。
- [x] 非线性模型必须稳定优于简单线性基准后才允许进入模拟交易。

## 八、P2：AI 因子研究助手

- [x] 新增结构化 AI 提案 schema，至少包含经济假设、目标市场、持有期、输入字段、可用延迟、DSL AST、预期方向、适用状态、失效条件和建议证伪实验。
- [x] AI 提案前向模型提供数据目录、现有因子定义、相关性簇、失败原因和剩余复杂度预算，减少重复生成已知公式。
- [x] AI 只能提交候选到待审队列；DSL 校验器、数据引擎和统计门禁负责计算与结论。
- [x] AI 不可看到最终锁定确认集标签、隐含收益排名或未公开统计答案。
- [x] 对每个 AI 候选保存供应商、模型、提示词版本、温度、输入指纹、token 用量和输出原文。
- [x] AI 审阅继续保持 `statistical_status` 锁定，只能解释证据、指出风险和提出下一项可证伪测试。
- [x] 增加失败学习：把“重复公式、未来函数、覆盖不足、成本过高、状态不稳”等结构化原因反馈给下一轮提案。
- [x] 为 AI 搜索设置每轮候选上限、公式复杂度上限、重复率上限和停止条件。
- [x] 对比 AI、人工模板、随机 DSL 和符号回归的发现效率，不能只报告 AI 的胜出样例。
- [x] 当 LLM 不可用时，确定性发现、复验和历史恢复流程必须完全可运行。

### AI 助手验收标准

- [x] AI 生成的非法或含未来信息公式无法进入计算队列。
- [x] AI 无法直接把因子状态升级为 `research_passed` 或 `trading_validated`。
- [ ] 在固定候选预算下，AI 相对随机 DSL 的有效且非重复候选比例有可复现提升。

## 九、P2：公告、新闻与事件因子

- [x] 定义时点事件数据契约，区分事件发生时间、公开时间、采集时间和修订时间。
- [x] 使用 LLM 将公告和新闻抽取为固定事件 taxonomy，不直接输出涨跌预测。
- [x] 首批事件覆盖业绩预告、盈利修正、回购、增减持、分红、监管处罚、重大合同和停复牌。
- [x] 每个事件输出实体、事件类型、方向、强度、置信度、证据片段和可用时间。
- [x] 低置信度、实体不一致或无法确认发布时间的事件不得进入因子计算。
- [x] 事件因子使用事件研究和横截面验证，分别报告公告后 1、3、5、10、20 日残差收益。
- [x] 防止同一公告被多家媒体转载后重复计数，并记录聚类规则版本。
- [x] 将事件因子与价格、成交量和流动性状态组合，检验条件效应而非只看平均收益。

### 事件因子验收标准

- [ ] 人工标注样本上的事件类型、实体和时间准确率达到预注册门槛。
- [x] 新闻发布时间延迟一分钟也会按新的 `available_time` 重新生成研究快照。
- [x] 事件研究结果能定位到原始文本证据，但导出内容不泄露密钥或受限数据。

## 十、P2：首批候选因子实验队列

- [x] 实验 1：复验 `ADX direction`，比较 ADX 10/14/20、持有期 1/3/5/10 日和行业中性残差收益。
- [x] 实验 2：研究波动调整残差动量，比较 20/60/120 日动量除以实现波动率后的稳定性。
- [x] 实验 3：研究成交额冲击条件下的突破延续，约束涨跌停、次日可成交性和换手成本。
- [x] 实验 4：研究 1 至 5 日短期反转，区分正常回撤、流动性冲击和公告跳空。
- [x] 实验 5：拆分隔夜与日内收益，检验开盘缺口延续和日内反转是否具有独立增量 IC。
- [ ] 实验 6：研究 Amihud 非流动性、成交额分位和换手突变，并验证小市值暴露中性化后的结果。
- [x] 实验 7：研究涨停后延续、开板和封板强度，显式模拟次日涨停无法买入。
- [ ] 实验 8：研究公告事件与价格状态的交互，例如业绩上修叠加中期趋势或超跌状态。

### 首批实验通过标准

- [x] 每个实验在运行前保存主假设、主标签、参数预算、目标市场和通过门槛。
- [x] 先完成现有 `ADX direction` 的横截面证伪，再扩大 AI 候选搜索空间。
- [x] 任一实验即使失败也保留完整结果，并可阻止 AI 再次提交等价候选。

## 十一、P2：研究工作台与人工审核

- [x] 新增“候选收件箱”，区分人工、AI、模板、随机 DSL 和符号回归来源。
- [x] 候选详情展示经济假设、公式 AST、数据需求、重复风险、未来信息检查和预计计算成本。
- [x] 研究人员必须先批准候选和预算，系统才能启动大规模搜索。
- [x] 结果页分开展示探索分数、研究状态、交易状态和 AI 审阅，避免单一总分误导。
- [ ] 增加因子相关性簇、参数平台、年度稳定性、市场状态和增量价值视图。
- [x] 增加确认集开启确认流程，明确展示这是不可逆的研究事件并写入审计记录。
- [x] 提供因子退役、替代关系和组合影响预览，不静默删除历史定义。

### 工作台验收标准

- [x] 用户能够从 AI 假设一路追溯到 DSL、数据、试验、统计结论、组合决策和模拟运行。
- [x] 页面不会用“推荐”“最佳因子”等文字描述仅在训练期胜出的候选。
- [x] 未通过因果或数据检查的候选不会出现“开始回测”入口。

## 十二、P3：模拟交易与持续失效监控

- [x] `trading_validated` 因子先进入不少于一个完整再平衡周期的影子组合或模拟交易。
- [x] 实时记录信号时间、可成交时间、理论价格、实际模拟价格、滑点、拒单原因和容量占用。
- [x] 对比研究期和模拟期的 IC、覆盖率、换手、成本、成交率和收益归因。
- [x] 增加 IC 衰减、方向翻转、覆盖下降、数据漂移、成本突破和容量下降告警。
- [x] 漂移监控使用 PSI、分布距离和因子相关结构变化，但告警阈值按因子预注册。
- [x] 因子降级后禁止新策略版本采用，已有实验保留其锁定定义和历史结果。
- [x] 制定 `degraded` 到 `retired` 的观察周期、恢复条件和人工复核规则。
- [x] 实盘接入仍保持关闭，除非另行完成券商适配、密钥治理、风控和合规发布门禁。

### 模拟验证验收标准

- [x] 模拟期成本后表现、成交率和容量均达到预注册目标后，状态才可升级为 `trading_validated`。
- [x] 研究收益与模拟收益差异可以分解为信号衰减、数据延迟、成交、成本和组合约束。
- [x] 因子失效后一个调度周期内完成告警、降级和受影响策略定位。

## 十三、里程碑与发布顺序

### M1：结论可信

- [x] 完成全部 P0 口径修复、回归测试和状态边界调整。
- [x] 完成真实 A 股股票池、时点数据检查和现有 14 因子横截面基线。
- [x] 发布前复核历史结果兼容策略，旧记录明确标注旧引擎口径。

### M2：可以规模化发现

- [x] 完成因子注册表、安全 DSL、确定性发现流水线和全局试验账本。
- [x] 完成首批 8 个预注册实验中的至少 4 个，并保留成功与失败证据。
- [x] 完成组合增量价值和目标市场门禁。

### M3：AI 可控接入

- [x] 完成结构化 AI 提案、候选预算、失败反馈和模型可追溯性。
- [ ] 完成 AI、人工模板、随机 DSL 和符号回归的公平对照实验。
- [x] 完成公告新闻事件因子的时点验证。

### M4：交易验证闭环

- [x] 完成研究工作台、影子组合、模拟交易和持续失效监控。
- [ ] 至少一个因子完整走过 `draft` 到 `trading_validated`，并能恢复全部证据。
- [x] 发布说明明确区分研究通过、模拟通过和实盘可用，禁止将模拟结果描述为实盘收益。

## 十四、全局发布门禁

- [x] 后端单元测试、集成测试、数据防泄漏测试和属性测试全部通过。
- [x] 前端测试、TypeScript 类型检查和生产构建全部通过。
- [x] 使用人工构造数据证明系统能够识别未来函数、重叠收益、存活偏差和重复试验。
- [x] 使用至少一个真实 A 股股票池完成可复现的端到端只读回归。
- [x] 数据、公式、试验、模型、提示词、成本和结果均有版本与哈希。
- [x] AI 服务中断、超时或返回非法结构时，确定性研究流程和历史证据不受影响。
- [ ] 所有“通过”状态都展示原始证据、门禁规则、目标市场和剩余风险。
- [x] 文档、API schema、数据库迁移、前端类型和导出格式保持一致。
- [x] 发布前确认运行中 API 的版本与源码构建标识一致。

## 十五、明确不做

- [x] 不把 LLM 对 K 线的文字判断直接作为交易因子。
- [x] 不允许 AI 自己生成、运行和修改任意 Python 回测代码。
- [x] 不以单股票、单时间窗口或单次高收益作为因子有效证据。
- [x] 不把训练集、发现集和最终确认集反复混用。
- [x] 不因路线图功能已实现就宣称已有真实研究证据。
- [x] 不在没有真实成本、容量和成交约束时标记为 `trading_validated`。
- [x] 不在本路线图中开启真实账户自动交易。
