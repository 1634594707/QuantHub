# QuantHub AI 因子发现与交易验证路线图

> 更新日期：2026-08-11
>
> 前置基础：[FACTOR_RESEARCH_ROADMAP.md](FACTOR_RESEARCH_ROADMAP.md) 已完成因子复验、持久化、成本、执行约束和研究治理能力。
>
> 本路线图目标：把固定的 14 个技术因子筛选器升级为可扩展、可审计、能够借助 AI 生成研究假设，但由确定性统计程序决定结论的因子发现系统。
>
> 状态说明：所有 `[ ]` 均为尚未完成的任务；只有代码、测试、真实数据运行和证据留存全部通过后才能改为 `[x]`。

## 当前主路线：BRAIN 式 Alpha 发现到 7 天模拟验证

这条路线以用户现有的回测与回撤逻辑为唯一评价器。AI 不负责判断因子好坏，只负责提出结构化 Alpha 表达式；所有候选都必须进入同一套确定性门禁。

- [x] 使用 WorldQuant BRAIN 类似的“表达式批次”作为候选入口，支持 AI 提案与确定性语法搜索混合生成。
- [x] 研究入口允许选择目标市场与标的；加密资产支持 OKX 主流永续/现货，A 股支持输入六位股票代码并通过 AkShare 取数。
- [x] 支持手工填写安全函数式 Alpha 表达式，或上传包含 `expression` / `formula_ast` 的 JSON 候选批次进行统一回测。
- [x] AI 只输出白名单 DSL 的 JSON AST，禁止任意 Python、未来数据、负滞后和动态代码执行。
- [x] 相同请求与行情快照先命中候选批次指纹，直接复用原批次，禁止重复调用 AI 后更换公式。
- [x] 候选批次统一执行发现集预检、滚动验证、双倍成本压力、回撤门禁和一次性锁定确认。
- [x] 保存候选来源、公式哈希、模型、提示词版本、输入指纹、token 用量、AI 原始输出、失效条件和证伪测试。
- [x] 通过研究门禁的因子可自动打包并进入 OKX Demo；Demo 请求强制要求至少 7 个真实自然日。
- [x] 模拟观察持续记录信号、理论价格、内部镜像订单、OKX Demo 订单、成交率、手续费、资金费率、持仓、账户收益、回撤、风险和对账证据。
- [ ] 至少一个真实因子通过锁定确认并在 OKX Demo 完成不少于 7 天的连续前向观察。
- [ ] 观察期结束后完成研究收益与模拟收益差异归因，并由成交、成本、容量、回撤、风险和对账门禁决定是否进入 `trading_validated`。

固定执行顺序：`生成并锁定 Alpha 批次 → 用现有逻辑批量回测/回撤 → 选出通过门禁的因子 → OKX Demo 至少 7 天 → 记录收益和完整证据 → 决定是否保留或降级`。

### 当前证据快照

- 真实 A 股基线：`demo_artifacts/a_share_300_factor_baseline.json`，300 标的、516 个共同 ordinal session、14 个因子，SHA256 `c08cf40b23f3d066b42de178343382cd59ef186cc3a3edae144b3032b34e4e38`。
- 预注册实验：`demo_artifacts/a_share_preregistered_experiments_01_07.json`，实验 1/2/3/4/5/7 共 6 个，1 个通过、5 个失败，SHA256 `2ec29d9b6ba70e111c844e9204ae5a8c05defb0953c0dbac491be08a39775c9b`。
- 最新 `600519` 运行：`6ba1a0b8e1174b53a92c7b23c2fdb8db`，`usable_factors=0`、`multifactor_constructed=false`。
- 证据边界：现有 session 不是可声明的自然日期；历史退市/ST/指数成员、时点行业/市值/Beta 暴露和人工新闻标注仍缺失。所有模拟审计均固定 `live_trading_enabled=false`。

## 一、核心原则与状态边界

- [x] 将因子生命周期统一为 `draft`、`exploratory`、`research_passed`、`trading_validated`、`degraded`、`retired`，并为每次状态变化保存规则和证据。
- [x] 将“训练期被选中”改名为“探索候选”，禁止它直接表示统计通过或交易可用。
- [x] `research_passed` 只由锁定的样本外统计门禁产生，AI、人工备注和训练期排名均不得修改该状态。
- [x] `trading_validated` 必须额外通过目标市场的成本、容量、执行约束和组合增量价值验证。
- [x] 将“四个市场全部通过”改为可配置规则：目标市场通过是强制项，跨市场迁移只作为可选的稳健性证据。
- [x] AI 不读取最终锁定确认集的收益标签，不直接生成买卖指令，不运行任意 Python，不修改程序统计结论。

### 状态边界验收标准

- [x] 当一个研究批次没有 `research_passed` 因子时，系统显示“未发现合格因子”，且不能创建可交易策略实验。
- [x] 任一因子状态都能追溯到公式版本、数据快照、试验次数、验证窗口、成本档案和门禁版本。
- [x] 同一因子可以在 A 股通过、在加密资产失败，不因跨市场失败而覆盖目标市场结论。

## 二、P0：修正会改变研究结论的口径

- [x] 修正横截面 `horizon > 1` 时把重叠未来收益按日连续复利的问题。
- [x] 提供两种无重叠实现：每 `horizon` 日调仓，以及按调仓日拆分为 `horizon` 个持仓批次的 cohort portfolio。
- [x] 交易成本按真实调仓批次和实际换手扣除，不再对重叠的未来收益直接逐日扣费。
- [x] 横截面 IC 显著性使用 HAC/Newey-West 或 block bootstrap，最大滞后至少覆盖 `horizon - 1`。
- [x] 横截面同批和跨批候选统一纳入多重检验，不只校正当前页面展示的因子。
- [x] 将 A 股结果拆分为“多头最高分组相对基准”“多头加指数对冲”和“理论多空”，禁止把不可执行的底部做空默认标记为可交易。
- [x] 没有合格因子时，移除当前按训练 IC 回退进入多因子组合的逻辑；如需保留，必须标记为仅探索且不输出交易状态。
- [x] 合并完全等价或仅差常数倍的公式族，例如 `mean_reversion` 与 `bollinger_reversal`，避免重复假设影响多重检验和排名。
- [x] 将默认横截面门槛从“20 个日期、3 个标的”升级为按市场配置的有效样本门槛，并展示 `effective_dates`。
- [x] 为收益重叠、成本重复扣除、不可做空、候选回退和等价因子增加回归测试。

### P0 验收标准

- [x] 使用人工构造的 5 日收益样例时，cohort portfolio 与独立逐日持仓账本结果一致。
- [x] `horizon=1` 与现有非重叠结果保持兼容，`horizon=5` 不再产生重复复利。
- [x] 最新 `600519` 数据没有合格因子时，多因子结果明确显示“未构建”，不会混入两个 `reject` 因子。
- [x] A 股研究报告不会把理论空头收益计入默认可执行收益。

## 三、P0：建立真实的时点横截面研究底座

- [x] 建立第一个真实 A 股研究股票池，初始目标不少于 300 个标的、5 年历史和 500 个有效交易日。
- [ ] 股票池按日期保存成分，纳入退市、暂停上市、ST、停牌、上市不足期和历史指数调入调出信息。
- [x] 记录每个字段的 `event_time`、`available_time`、来源、抓取时间、复权方式和修订版本。
- [ ] 市值、行业、Beta 和基本面暴露使用对应日期可获得的数据，禁止用当前静态值回填历史。
- [x] 对行情、股票池和暴露数据执行覆盖率、重复、缺失、异常跳变和时间顺序检查。
- [x] 建立 A 股可交易性过滤：涨跌停、停牌、ST、上市天数、成交额、整手和 T+1。
- [x] 将每次横截面运行锁定到股票池快照、行情快照和暴露快照的哈希。
- [x] 用现有 14 个因子完成第一次真实横截面基线运行，并保存失败标的和缺失原因。
- [x] 为美股、加密资产和 MT5 分别定义独立股票池与执行语义，不复制 A 股门槛。

### 数据底座验收标准

- [x] 任意历史日期都能恢复当日真实可选股票集合，不能只看到今天仍存在的标的。
- [x] 任意基本面或事件字段都不能早于其 `available_time` 进入因子计算。
- [x] 第一次 A 股基线运行覆盖率、缺失率、中性化失败率和不可交易比例均可解释并可回链。

## 四、P1：因子注册表与安全 DSL

- [x] 新增不可变 `FactorDefinition` 注册表，保存因子 ID、名称、市场、输入字段、AST、参数、方向假设、持有期、可用延迟和经济逻辑。
- [x] 每个公式使用规范化 AST 哈希作为身份，参数或算子变化必须产生新版本。
- [x] DSL 首批只允许白名单算子：四则运算、比较、条件、滞后、差分、滚动统计、排名、截尾、标准化和行业中性化。
- [x] DSL 明确禁止负数滞后、未来窗口、全样本标准化和任何未声明外部函数。
- [x] 增加单位与形状检查，阻止价格直接与成交量相加等无意义表达式。
- [x] 增加复杂度预算，包括最大 AST 深度、最大算子数、最大参数组合数和最小数据覆盖率。
- [x] 建立公式族概念，将同一经济假设的参数变体计为同一试验族。
- [x] 对候选执行完全重复、单调等价、常数倍等价和高相关冗余检测。
- [x] 将现有 14 个技术因子迁移到注册表和 DSL，同时保持历史版本可恢复。
- [x] AlphaGPT 和 AlphaMaster 的 token 公式通过适配层映射到统一因子定义，不再形成独立的不可审计搜索路径。

### DSL 验收标准

- [x] 任一 AI 或人工候选必须先通过语法、因果、单位、复杂度和数据覆盖检查，之后才能进入计算队列。
- [x] 修改参数、公式、数据可用延迟或预处理方式后，因子版本和哈希必然变化。
- [x] `mean_reversion` 与仅差常数倍的公式会归入同一等价族。

## 五、P1：确定性因子发现与验证流水线

- [x] 将数据拆分为发现集、滚动验证集和最终锁定确认集，确认集只允许在预注册实验完成后开启一次。
- [x] 使用 purged walk-forward 和 embargo，隔离期覆盖标签持有期及数据发布时间延迟。
- [x] 支持按因子族进行有限参数网格搜索，但把全部参数尝试计入试验次数。
- [x] 增加参数平台测试，只有相邻参数区间均有效时才认为结果稳健。
- [x] 增加时间稳定性测试：年度、牛熊、趋势、震荡、高低波动和流动性状态。
- [x] 增加横截面稳定性测试：行业、市值分组、流动性分组和上市年龄分组。
- [x] 增加标签稳定性测试：未来 1、3、5、10、20 日残差收益和风险调整收益。
- [x] 增加安慰剂测试：标签打乱、时间平移、随机因子和伪事件日期。
- [x] 增加数据扰动测试：缺失、价格微扰、成本上浮、延迟成交和容量收缩。
- [x] 使用市场和行业中性后的残差收益作为主要横截面标签，原始收益作为辅助指标。
- [x] 因子排序采用 Pareto 前沿，不用单一综合分掩盖 IC、换手、容量、稳定性和相关性的取舍。
- [x] 保存所有失败候选及失败原因，禁止只保存最终胜出者。

### 发现流水线验收标准

- [x] 研究人员无法通过反复查看确认集结果继续调参而不留下新的试验记录。
- [x] 只有在多数窗口、主要分组和参数邻域均保持方向一致的因子才能进入 `research_passed`。
- [x] 标签打乱后，系统发现合格因子的比例符合设定的假阳性上限。

## 六、P1：全局试验账本与数据窥探控制

- [x] 新增 `FactorExperiment` 账本，记录假设、候选来源、父因子、公式族、数据范围、参数、模型、提示词、运行状态和结果。
- [x] 每一次人工修改、AI 生成、参数搜索和失败重试都生成不可变试验记录。
- [x] Benjamini-Hochberg 校正覆盖同一研究计划内的全部历史候选，而非单次 API 请求。
- [x] 增加 Deflated Sharpe Ratio，修正非正态收益和多次尝试造成的夏普膨胀。
- [x] 对大规模搜索批次增加 White Reality Check、SPA 或 block bootstrap 基准比较。
- [x] 在报告中同时展示原始 p 值、批次校正值、全局校正值、有效样本量和累计尝试次数。
- [x] 预注册每个实验的主指标、次指标、通过门槛、数据范围和最大候选预算。
- [x] 为研究计划设置候选数、计算量、LLM token 和确认集开启次数预算。

### 试验治理验收标准

- [x] 删除失败结果或拆分请求不能降低一个研究计划的累计试验次数。
- [x] AI 批量生成 100 个候选时，统计门禁按 100 个及其参数变体进行校正。
- [x] 历史研究可以完整回答“这个结论之前尝试过多少个公式和参数”。

## 七、P1：因子组合与真实增量价值

- [x] 计算候选与现有因子的 Pearson、Spearman、尾部和分市场状态相关性。
- [x] 对候选执行相对已部署因子的正交化，报告原始 IC 和增量 IC。
- [x] 使用时间序列外层验证组合权重，禁止根据最终确认集选择权重。
- [x] 建立简单基准：等权、IC 权重、风险平价和带正则约束的线性组合。
- [x] LightGBM、CatBoost 或其他非线性模型只能作为对照组，并使用嵌套时间序列验证。
- [x] 报告组合相对单因子的收益增量、换手增量、容量变化、回撤变化和尾部风险。
- [x] 将单因子有效但组合无增量价值的结果标记为“研究有效、组合不采用”。
- [x] 按目标市场配置权重、行业偏离、单股上限、成交额参与率和换手预算。

### 组合验收标准

- [x] 新因子必须在扣除增量成本后改善至少一个预注册组合目标，且不能突破风险硬约束。
- [x] 高度相关的因子不能仅因名称或公式不同重复获得权重。
- [x] 非线性模型必须稳定优于简单线性基准后才允许进入模拟交易。

## 八、P2：AI 因子研究助手

- [x] 新增结构化 AI 提案 schema，至少包含经济假设、目标市场、持有期、输入字段、可用延迟、DSL AST、预期方向、适用状态、失效条件和建议证伪实验。
- [x] AI 提案前向模型提供数据目录、现有因子定义、相关性簇、失败原因和剩余复杂度预算，减少重复生成已知公式。
- [x] AI 只能提交候选到待审队列；DSL 校验器、数据引擎和统计门禁负责计算与结论。
- [x] AI 不可看到最终锁定确认集标签、隐含收益排名或未公开统计答案。
- [x] 对每个 AI 候选保存供应商、模型、提示词版本、温度、输入指纹、token 用量和输出原文。
- [x] AI 审阅继续保持 `statistical_status` 锁定，只能解释证据、指出风险和提出下一项可证伪测试。
- [x] 增加失败学习：把“重复公式、未来函数、覆盖不足、成本过高、状态不稳”等结构化原因反馈给下一轮提案。
- [x] 为 AI 搜索设置每轮候选上限、公式复杂度上限、重复率上限和停止条件。
- [x] 对比 AI、人工模板、随机 DSL 和符号回归的发现效率，不能只报告 AI 的胜出样例。
- [x] 当 LLM 不可用时，确定性发现、复验和历史恢复流程必须完全可运行。

### AI 助手验收标准

- [x] AI 生成的非法或含未来信息公式无法进入计算队列。
- [x] AI 无法直接把因子状态升级为 `research_passed` 或 `trading_validated`。
- [ ] 在固定候选预算下，AI 相对随机 DSL 的有效且非重复候选比例有可复现提升。

## 九、P2：公告、新闻与事件因子

- [x] 定义时点事件数据契约，区分事件发生时间、公开时间、采集时间和修订时间。
- [x] 使用 LLM 将公告和新闻抽取为固定事件 taxonomy，不直接输出涨跌预测。
- [x] 首批事件覆盖业绩预告、盈利修正、回购、增减持、分红、监管处罚、重大合同和停复牌。
- [x] 每个事件输出实体、事件类型、方向、强度、置信度、证据片段和可用时间。
- [x] 低置信度、实体不一致或无法确认发布时间的事件不得进入因子计算。
- [x] 事件因子使用事件研究和横截面验证，分别报告公告后 1、3、5、10、20 日残差收益。
- [x] 防止同一公告被多家媒体转载后重复计数，并记录聚类规则版本。
- [x] 将事件因子与价格、成交量和流动性状态组合，检验条件效应而非只看平均收益。

### 事件因子验收标准

- [ ] 人工标注样本上的事件类型、实体和时间准确率达到预注册门槛。
- [x] 新闻发布时间延迟一分钟也会按新的 `available_time` 重新生成研究快照。
- [x] 事件研究结果能定位到原始文本证据，但导出内容不泄露密钥或受限数据。

## 十、P2：首批候选因子实验队列

- [x] 实验 1：复验 `ADX direction`，比较 ADX 10/14/20、持有期 1/3/5/10 日和行业中性残差收益。
- [x] 实验 2：研究波动调整残差动量，比较 20/60/120 日动量除以实现波动率后的稳定性。
- [x] 实验 3：研究成交额冲击条件下的突破延续，约束涨跌停、次日可成交性和换手成本。
- [x] 实验 4：研究 1 至 5 日短期反转，区分正常回撤、流动性冲击和公告跳空。
- [x] 实验 5：拆分隔夜与日内收益，检验开盘缺口延续和日内反转是否具有独立增量 IC。
- [ ] 实验 6：研究 Amihud 非流动性、成交额分位和换手突变，并验证小市值暴露中性化后的结果。
- [x] 实验 7：研究涨停后延续、开板和封板强度，显式模拟次日涨停无法买入。
- [ ] 实验 8：研究公告事件与价格状态的交互，例如业绩上修叠加中期趋势或超跌状态。

### 首批实验通过标准

- [x] 每个实验在运行前保存主假设、主标签、参数预算、目标市场和通过门槛。
- [x] 先完成现有 `ADX direction` 的横截面证伪，再扩大 AI 候选搜索空间。
- [x] 任一实验即使失败也保留完整结果，并可阻止 AI 再次提交等价候选。

## 十一、P2：研究工作台与人工审核

- [x] 新增“候选收件箱”，区分人工、AI、模板、随机 DSL 和符号回归来源。
- [x] 候选详情展示经济假设、公式 AST、数据需求、重复风险、未来信息检查和预计计算成本。
- [x] 研究人员必须先批准候选和预算，系统才能启动大规模搜索。
- [x] 结果页分开展示探索分数、研究状态、交易状态和 AI 审阅，避免单一总分误导。
- [ ] 增加因子相关性簇、参数平台、年度稳定性、市场状态和增量价值视图。
- [x] 增加确认集开启确认流程，明确展示这是不可逆的研究事件并写入审计记录。
- [x] 提供因子退役、替代关系和组合影响预览，不静默删除历史定义。

### 工作台验收标准

- [x] 用户能够从 AI 假设一路追溯到 DSL、数据、试验、统计结论、组合决策和模拟运行。
- [x] 页面不会用“推荐”“最佳因子”等文字描述仅在训练期胜出的候选。
- [x] 未通过因果或数据检查的候选不会出现“开始回测”入口。

## 十二、P3：模拟交易与持续失效监控

- [x] `trading_validated` 因子先进入不少于一个完整再平衡周期的影子组合或模拟交易。
- [x] 实时记录信号时间、可成交时间、理论价格、实际模拟价格、滑点、拒单原因和容量占用。
- [x] 对比研究期和模拟期的 IC、覆盖率、换手、成本、成交率和收益归因。
- [x] 增加 IC 衰减、方向翻转、覆盖下降、数据漂移、成本突破和容量下降告警。
- [x] 漂移监控使用 PSI、分布距离和因子相关结构变化，但告警阈值按因子预注册。
- [x] 因子降级后禁止新策略版本采用，已有实验保留其锁定定义和历史结果。
- [x] 制定 `degraded` 到 `retired` 的观察周期、恢复条件和人工复核规则。
- [x] 实盘接入仍保持关闭，除非另行完成券商适配、密钥治理、风控和合规发布门禁。

### 模拟验证验收标准

- [x] 模拟期成本后表现、成交率和容量均达到预注册目标后，状态才可升级为 `trading_validated`。
- [x] 研究收益与模拟收益差异可以分解为信号衰减、数据延迟、成交、成本和组合约束。
- [x] 因子失效后一个调度周期内完成告警、降级和受影响策略定位。

## 十三、里程碑与发布顺序

### M1：结论可信

- [x] 完成全部 P0 口径修复、回归测试和状态边界调整。
- [x] 完成真实 A 股股票池、时点数据检查和现有 14 因子横截面基线。
- [x] 发布前复核历史结果兼容策略，旧记录明确标注旧引擎口径。

### M2：可以规模化发现

- [x] 完成因子注册表、安全 DSL、确定性发现流水线和全局试验账本。
- [x] 完成首批 8 个预注册实验中的至少 4 个，并保留成功与失败证据。
- [x] 完成组合增量价值和目标市场门禁。

### M3：AI 可控接入

- [x] 完成结构化 AI 提案、候选预算、失败反馈和模型可追溯性。
- [ ] 完成 AI、人工模板、随机 DSL 和符号回归的公平对照实验。
- [x] 完成公告新闻事件因子的时点验证。

### M4：交易验证闭环

- [x] 完成研究工作台、影子组合、模拟交易和持续失效监控。
- [ ] 至少一个因子完整走过 `draft` 到 `trading_validated`，并能恢复全部证据。
- [x] 发布说明明确区分研究通过、模拟通过和实盘可用，禁止将模拟结果描述为实盘收益。

## 十四、全局发布门禁

- [x] 后端单元测试、集成测试、数据防泄漏测试和属性测试全部通过。
- [x] 前端测试、TypeScript 类型检查和生产构建全部通过。
- [x] 使用人工构造数据证明系统能够识别未来函数、重叠收益、存活偏差和重复试验。
- [x] 使用至少一个真实 A 股股票池完成可复现的端到端只读回归。
- [x] 数据、公式、试验、模型、提示词、成本和结果均有版本与哈希。
- [x] AI 服务中断、超时或返回非法结构时，确定性研究流程和历史证据不受影响。
- [ ] 所有“通过”状态都展示原始证据、门禁规则、目标市场和剩余风险。
- [x] 文档、API schema、数据库迁移、前端类型和导出格式保持一致。
- [x] 发布前确认运行中 API 的版本与源码构建标识一致。

## 十五、明确不做

- [x] 不把 LLM 对 K 线的文字判断直接作为交易因子。
- [x] 不允许 AI 自己生成、运行和修改任意 Python 回测代码。
- [x] 不以单股票、单时间窗口或单次高收益作为因子有效证据。
- [x] 不把训练集、发现集和最终确认集反复混用。
- [x] 不因路线图功能已实现就宣称已有真实研究证据。
- [x] 不在没有真实成本、容量和成交约束时标记为 `trading_validated`。
- [x] 不在本路线图中开启真实账户自动交易。

## 十六、因子工厂产品闭环（2026-08-10 用户确认）

> 产品目标：提供一个统一窗口完成“发现候选因子、回测验证、OKX 模拟账户试验、因子登记与持续监控”。研究结论、模拟表现和实盘可用性必须分开，不以一次高收益代替完整证据。

### F1：候选因子挖掘窗口

- [x] 将 `/factor-research` 恢复为“因子工厂”正式入口，归属“市场研究”工作区。
- [x] 增加候选挖掘视图，首批提供波动率调整动量、流动性冲击反转、成交量确认突破三个可审计 DSL 模板。
- [x] 候选创建前记录名称、市场、周期、研究角度、事前经济假设、失效条件和数据字段。
- [x] 候选必须通过未来信息、因果逻辑、数据可用性和研究预算确认，才能登记为不可变 `draft` 定义。
- [ ] 接入人工、AI、模板、随机 DSL、符号回归五类候选的持久化收件箱和批量审核。
- [x] 增加重复公式、相关性簇和计算预算的实时预检，阻止重复或超预算候选进入回测。

### F2：候选回测与研究门禁

- [x] 让任意已登记 DSL 因子进入统一回测执行器，不再局限于内置因子集合。
- [x] 为每个候选保存发现集、滚动验证集和锁定确认集的范围与数据指纹。
- [ ] 展示样本外收益、IC、成本后表现、最大回撤、参数平台、年度稳定性、市场状态和跨标的一致性。
- [x] 将门禁阈值预注册；未通过候选记录失败原因，不允许通过调参覆盖原始失败证据。
- [x] 通过后按证据将生命周期从 `draft` 推进到 `exploratory` 和 `research_passed`。

### F3：OKX Demo 因子试验

- [x] 将 `research_passed` 因子锁定版本后绑定 Runner 策略、仓位映射和风险参数。
- [x] 在 OKX Demo 记录信号时间、理论价格、订单、成交、滑点、手续费、资金费率、持仓和账户收益。
- [ ] 至少运行一个完整再平衡周期，并完成研究收益与模拟收益差异归因。
- [x] 模拟成交率、成本后表现、容量、回撤和账实一致性达到预注册门槛后，才允许升级为 `trading_validated`。
- [x] Kill Switch、订单对账和账户级风控未完成前，禁止无人值守自动下单。

### F4：因子档案与持续监控

- [x] 建立已验证因子档案，分别保存“事前假设”和“事后证据”，禁止事后叙事覆盖原始假设。
- [x] 因子必须在同一笔模拟运行中完成至少 7 个真实自然日并通过全部模拟门禁，才可进入可选档案；研究记录与短期模拟默认不展示为档案。
- [x] 档案展示公式版本与哈希、数据来源、适用市场/标的/周期、通过原因、失效条件、剩余风险和完整证据链。
- [x] 持续监控 IC 衰减、方向翻转、覆盖率、成本、容量、成交率和模拟收益偏差。
- [x] 达到失效门槛时自动进入 `degraded`，经人工复核后恢复或进入 `retired`。

### F2/F4 实现证据（2026-08-11）

- `apps/api/domains/factor_factory/service.py` 将候选固定在研究计划预算内，分为发现集、滚动验证集和锁定确认集；30 个名额覆盖六类参数化 DSL、MACD/ADX/RSI/布林/OBV/Chaikin 等 11 个既有公式，以及一个因果安全的符号区间位置公式，来源明确标记为 `template`、`random_dsl` 或 `symbolic_regression`。
- 候选进入实验前只用发现集执行公式哈希、同方向常数倍/单调等价和跨研究族极高相关预检；同族参数平台候选保留。相同数据指纹、请求参数、候选宇宙和研究引擎版本只返回原运行，禁止重复开启锁定确认集。
- `apps/api/store.py` 的 `factor_factory_runs`、`factor_factory_candidates` 和 `factor_factory_observations` 表保存运行、候选、生命周期回链与逐 K 线模拟权益快照；服务重启后后台观察线程会恢复 `paper_observing` 运行。
- 研究通过后会自动创建 `factor-factory:<run_id>` 专属 `simulation_orders` 账户；每个新 K 线调仓保存订单、成交、手续费、因子版本、研究运行号和再平衡周期。专属成交标记为 `isolated`，不会污染共享手工模拟账本；重复轮询不会重复下单。
- 观察期结束时保存研究确认期与前向模拟期的收益差异归因，锁定信号衰减、数据延迟、执行、成本和组合约束五类分解；只有观察期限、最小观察数、成本后收益、回撤、成交率、容量和执行审计全部通过才允许 `trading_validated`。
- `tests/test_factor_factory_automation.py` 与 `tests/test_factor_factory_demo.py` 覆盖全预算候选、发现集预检、研究指纹幂等、一次性确认集、模拟订单隔离、重复轮询幂等、收益归因、观察到期门禁、只读因子档案和应用内每日自动发现调度；最新因子工厂、生命周期与交易代理聚焦回归 83/83（另含 2 个子测试）、前端 108/108、因子工厂相关 Python lint、类型检查和生产构建通过。
- `GET /factor-factory/archive` 以因子定义为主键，只读聚合不可变公式、生命周期事件、预注册实验、实验结果事件、自动研究运行、锁定确认门禁、前向观察和模拟订单引用；默认 `eligible_only=true`，只有同一笔 `trading_validated` 运行具备至少 7 个真实自然日和完整模拟门禁证据才返回。`preregistration` 不包含结果事件，`post_study_evidence` 不覆盖原始假设，所有证据可由定义、实验、运行、数据快照和订单编号回链。
- 因子工厂页面已将旧的定义列表升级为紧凑证据台账，支持生命周期筛选，并列展示事前假设、失效条件、预注册指标/门槛与事后统计、确认结论、模拟收益和剩余风险。桌面 `1440×900` 与手机 `390×844` 浏览器验收均无横向溢出、文本裁切或控制台错误，截图与报告见 `docs/Plan/evidence/F4-factor-archive-web-2026-08-11/`。
- 2026-08-11 最新真实账本包含 321 个自动研究因子档案，`verified_count=0`；研究引擎 `2.5.0` 的构建 `a7d11b102e1a` 已部署到 8001，`restart_required=false`，Web、API 与 Demo Runner 均健康。部署后自动发现以 `already_ran_today` 跳过 1h/4h，没有重复研究或订单；全局风险 `normal`、未解决对账差异 0、未完成订单 0。当前账户余额快照已超过新鲜度阈值，未来若有因子通过锁定确认，必须先刷新账户与对账证据才允许激活 Demo。最新真实候选仍停在 `exploratory`，没有模拟观察或新 Demo 订单；F4 档案能力已就绪，但这不改变 F3 和整体目标仍需真实因子完成观察期的结论。
- 真实 OKX 证据：日线计划 `1478d6126b1743139b15b37bf4b68754` 为 `no_qualified_factor`；实时 4h 计划 `6cad1a7596fc4cc19fbd596d510762bd` 与长窗口扩展计划 `ae188ef18da04bc4bd944d050d6033aa` 均为 `no_research_passed_factor`；实时 1h 计划 `85ed6ad60ea44f8abc49e074c385531f` 与扩展计划 `c3d319bc709248299b30fcf5ec21d385` 为 `no_qualified_factor`。这些运行均未创建模拟订单，证明系统不会为满足路线图而绕过锁定确认门禁。
- 因子工厂后台现已内置每日 10:00 自动发现调度，只有以 `-Demo` 启动并显式设置 `QUANTHUB_FACTOR_AUTO_DISCOVERY=1` 时启用；没有 `paper_observing` 运行时，按 1h/2880 根和 4h/720 根、30 候选、原预注册门槛各尝试一次。当天已有运行、相同 K 线边界、已有观察中因子或非 Demo 环境都会跳过；研究入口使用进程内锁防止后台调度、API 请求和 Codex 心跳并发打开两个确认集。
- 构建 `fae7460dee1d` 的真实启动审计已证明应用内调度生效：1h/4h 均以 `already_ran_today` 跳过 `2026-08-11T04:00:00Z` 边界，最新运行号保持 `0ef0e01f918b416c961fe838bdde7349`，没有重复确认集或模拟订单。期间 OKX 曾短暂返回 `OnMaintenance`，未提交订单，恢复性对账随后通过；风险 `normal`、未解决差异 0、账户快照新鲜。证据见 `docs/Plan/evidence/F3-factor-auto-discovery-runtime-2026-08-11.json`。
- Codex 当前任务保留每日 10:00 心跳作为外部审计与故障恢复：核对应用调度结果、刷新真实观察、更新证据和测试，不再作为自动发现的唯一执行器；真实观察期完成前 F3 和整体目标保持未完成。

### F3 实现证据（2026-08-11）

- `apps/api/domains/factor_factory/okx_demo.py` 会把真正通过锁定确认的因子构建为签名、不可变且只允许 `demo` 环境运行的 `StrategyReleasePackage`；策略包固定因子 AST、数据指纹、成本假设、最大敞口、最大亏损和 Kill Switch 要求。
- Demo 激活前强制校验 Runner 环境、交易权限、时钟偏差、目标合约可解析且启用、USDT 结算、永续/交割产品类型、全局风险模式、账户快照新鲜度和未解决对账差异；不再限制为 BTC，支持 `NVDAUSDT` 规范化为 `NVDA-USDT-SWAP` 后动态预检。任一项失败只保存 `blocked` 证据，不提交订单。信号只映射到预注册敞口上限，合约张数按 `price × contract_size` 和交易所步长计算，确定性 intent id 防止重复下单；live 环境仍保留静态 BTC 白名单与独立审批。
- `apps/okx_runner` 与交易代理已提供资金费率、订单恢复、账户、风险和对账证据；观察结束还要求 Demo 订单进入最终状态、成交率达标、资金费率已记录、风险正常、对账清晰，以及 Demo 账户收益和回撤通过门禁，才允许 `trading_validated`。
- 因子工厂前端可选择 `OKX Demo` 或本地独立模拟；Demo 模式锁定真实公共行情通道，并展示 Runner 策略、订单/成交率、资金费率、风险和对账三态。刷新动作使用运行时持久化配置，不受当前表单值影响。
- `core/backtest/market_data.py` 的无起点实时取数改为从计算出的历史起点向前分页，按时间戳去重并检测游标推进；因子工厂把当前 UTC K 线边界作为 `end`，使 1h/4h 每个新周期使用独立缓存键，且无 `start` 的有界快照不足请求根数时也会失效重拉。`tests/test_market_data_live.py` 覆盖小页分页、K 线边界和有界/无界不完整缓存回退。
- 候选锁定确认不再把自身排名强制改成 1，滚动验证排名保持唯一；运行选择通过 `selected_factor_key` 单独记录，前端对历史重复排名记录也优先展示实际选中因子。
- 2026-08-11 Demo 运行预检证据：Runner 环境 `demo`、权限 `read_only + trade`、时钟偏差约 2 秒、`BTC-USDT-SWAP` 有效、账户快照新鲜、全局风险 `normal`、未解决对账差异 0；`live_trading_enabled=false`。
- 新真实 4h 计划 `3312e364b1f44e17a9aaa3000a76264b` 使用 720 根 BTC 公共行情和 30 个候选，首位 `ff_3312e364_short_term_10` 未通过锁定确认：确认收益、增量收益、夏普和 p 值均失败，增量收益 `-1.082%`。运行状态为 `no_research_passed_factor`，模拟观察 0、Demo 订单 0，F3 的真实订单与完整观察周期仍未完成。
- 长窗口 1h 计划 `3d72dc252dd540f28361148d150de7dd` 暴露并记录了旧分页只返回 1440/2880 根的问题，不作为有效长窗口证据。修复后计划 `3153e007e65f4d56af80435929ed0cc6` 实际取得 2880/2880 根、覆盖 `2026-04-13T04:00:00Z` 至 `2026-08-11T03:00:00Z`；首位短期反转因子滚动验证收益 `1.864%`、夏普 `1.3325`，但锁定确认收益 `-3.6364%`、夏普 `-6.6916`、增量收益 `-3.9962%`，仍正确停止且未创建 Demo 订单。
- 新 K 线边界计划 `2d4901169a0e4620b988ba8438da3cad` 使用 `requested_end=2026-08-11T04:00:00Z` 取得 2880/2880 根，数据指纹由上一计划的 `a1be04a6...` 更新为 `76a4b469...`；同一边界复跑以 `idempotent_replay=true` 返回原运行，没有再次开启确认集。30 个候选中 29 个通过发现集预检、1 个相关性簇候选被拒。选中的 40 期短期反转因子滚动验证收益 `4.2291%`、夏普 `3.0568`、最大回撤 `-4.1795%`，但锁定确认收益 `-1.8141%`、夏普 `-2.8390`、p 值 `0.4655`、相对买入持有增量收益 `-2.1413%`，状态为 `no_research_passed_factor`；模拟观察 0、Demo 新订单 0，证明新周期刷新不会绕过锁定确认门禁。
- 同一新边界的 4h 计划 `0ef0e01f918b416c961fe838bdde7349` 使用 720/720 根和新指纹 `600d83aa...`；30 个候选中 29 个通过预检。首位 10 期短期反转因子滚动验证收益 `5.7748%`、夏普 `4.4914`、最大回撤 `-1.5755%`，锁定确认收益却为 `-1.0491%`、夏普 `-2.1423`、p 值 `0.5828`、增量收益 `-1.2161%`，因此仍未进入 Demo；同边界复跑命中幂等，观察 0、Demo 新订单 0。1h/4h 运行、确认集编号、风险与对账快照已导出到 `docs/Plan/evidence/F3-factor-factory-live-research-2026-08-11.json`。
- 研究引擎 `2.4.0` 将滚动验证收益按时间顺序切为三个连续窗口，至少两个窗口成本后收益非负才允许开启锁定确认集；稳定窗口比例和窗口收益离散度同时进入候选排序。每个候选还必须在滚动验证集上通过 `2x` 手续费压力回测，压力后的收益、回撤、夏普和分段稳定性继续使用原预注册门槛，基础与压力场景的风险调整质量共同参与排序。滚动验证原始 p 值必须不高于预注册 `maximum_p_value`，Rank IC 方向必须与因子登记方向一致，避免统计证据不足的候选消耗唯一确认集。以上规则只读取发现集和滚动验证集，不访问确认标签，也不降低原收益、回撤、夏普或 p 值门槛；当前 `04:00 UTC` 数据边界不因版本升级重跑，等待下一真实数据指纹后启用。
- `2.4.0` 在当前边界完成不落库、不打开确认集的只读漏斗预演：1h 的 29 个预检候选全部被滚动验证门禁淘汰，首位 RSI 反转仅因发现/验证方向不一致被拒；4h 有 1 个候选通过，10 期短期反转验证收益 `5.7748%`、夏普 `4.4914`、p 值 `0.1593`、Rank IC `0.1201`，双倍成本收益 `4.3521%`、夏普 `3.4064`。预演明确记录 `confirmation_labels_accessed=false` 和 `formal_confirmation_opened=false`，证据见 `docs/Plan/evidence/F2-factor-factory-v24-preview-2026-08-11.json`。
- 新 1h 边界正式计划 `6701a5ce2c7d44f2a01e903d66a1ad6e` 使用 `requested_end=2026-08-11T05:00:00Z`、2880/2880 根真实 BTC 公共行情和新指纹 `7b54bb55...` 验证研究引擎 `2.4.0`。30 个候选中 29 个通过预检，但没有候选通过全部滚动验证与双倍成本门禁；首位 RSI 反转 20 的验证收益 `1.3413%`、夏普 `4.0841`、p 值 `0.1999`、Rank IC `0.0673`，双倍成本收益 `1.0366%`、夏普 `3.1829`，三段验证全部为正，却因发现集收益 `-1.5494%` 与验证集方向不一致而被拒。运行未开启锁定确认集，观察 0、模拟/Demo 订单 0；证据见 `docs/Plan/evidence/F2-factor-factory-v24-live-1h-2026-08-11.json`。
- 研究引擎 `2.5.0` 只调整 1h 候选供给，不改门禁、预算或 4h 宇宙：将反复弱势的六个 10/40 期动量、放量突破和 Donchian 变体替换为 12/24 期效率比趋势、高波动反转和量价收盘位置压力。使用正式计划 `6701a5ce...` 的同一真实指纹做只读预演时，29 个候选通过预检、0 个通过滚动验证；原 RSI 反转仍排第一且仍只因方向一致性失败，新增六个候选均未通过。预演没有读取确认集指标、没有落库或创建订单，聚焦回归 31/31 与 Ruff 通过；证据见 `docs/Plan/evidence/F2-factor-factory-v25-preview-2026-08-11.json`。该版本只作为下一真实 1h 指纹的候选多样性版本，不把本次预演声明为研究通过。

### 当前执行顺序

1. 在每个新真实行情指纹上生成并锁定一批 BRAIN 式安全 Alpha 表达式；AI 不可用时使用确定性语法搜索补足候选预算。
2. 用现有固定回测、回撤、滚动验证、双倍成本压力和锁定确认逻辑统一评估整批候选，不为获得通过结果降低任何门槛。
3. 只有真正达到 `research_passed` 的因子才进入 OKX Demo，并从首个真实模拟订单开始计时至少 7 天。
4. 每个观察周期持续记录收益、回撤、信号、订单、成交、费用、资金费率、风险和对账；不足 7 天不得提前宣告完成。
5. 观察期结束后通过模拟门禁才进入 `trading_validated`；实盘继续保持禁用，除非后续另行完成小额实盘准入。

### BRAIN 工作流 3.1.1 运行证据（2026-08-11）

- 研究工作台已支持选择虚拟货币或 A 股标的、填写研究方向、BRAIN/手工/JSON 批量/固定库候选模式；手工表达式和上传 JSON 都进入同一安全 DSL、固定回测、回撤和确认门禁。桌面 `1440x900` 与手机 `390x844` 验收通过，无横向溢出、文字裁切或控制台错误，证据见 `docs/Plan/evidence/F5-factor-brain-workflow-web-2026-08-11/`。
- 手工 JSON 批量运行 `bd10be9d0b5f44579b7f25dfa0e5f6f3` 使用 720 根真实 BTC 4h 公共行情评估两个 Alpha。均完成安全解析与滚动验证，但都被研究门禁拒绝；观察 0、模拟订单 0、`live_trading_enabled=false`。
- AI 协议升级为 `brain-alpha-json-v2`：提示词明确递归 AST 字段，服务端仅把常见 `args` JSON 参数形式规范化为标准 AST，再继续执行原白名单、因果性、单位和复杂度校验；被拒绝的 AI 原始输出也纳入审计。候选生成指纹包含挖掘器和提示词版本，协议更新不会错误复用旧运行。
- 3.1.1 BRAIN 运行 `381c9cc0b64e44daa1fdbcb9a19bbba6` 中，DeepSeek `deepseek-v4-flash` 成功生成 3/3 个安全 AI 候选，与 3 个符号回归和 2 个随机 DSL 候选共同完成固定回测。8 个候选均未通过门禁，因此没有选择因子、没有启动 7 天 Demo、没有订单；因子专项回归 39/39 与 Ruff 通过，运行构建与源码一致、Runner 为 `demo`、实盘关闭。
- 七日档案与动态 Demo 合约浏览器验收已在桌面 `1440x900` 和手机 `390x844` 通过：自定义 `NVDAUSDT` 时继续保持 `OKX Demo`，观察天数最小值固定为 7，档案筛选不再包含 `exploratory`，无横向溢出、文字裁切或控制台错误。证据见 `docs/Plan/evidence/F6-factor-archive-dynamic-demo-web-2026-08-11/`。当前真实账本有 398 条研究记录、0 条七日准入档案，实盘保持关闭；由于受限运行进程不能读取本机 Demo 凭据库，真实 NVDA Runner 预检需在下一次正常 Demo 服务重启后执行，不能用测试结果替代真实交易所证据。
- 研究入口已补齐参数与标的发现：虚拟货币可输入代码或中文/英文名称，例如“英伟达”回填 `NVDA-USDT-SWAP`；A 股可输入代码或名称，例如“贵州茅台”回填 `600519`，并继续合并本地标的主数据搜索结果。手工 Alpha 提供趋势动量、短期反转、量价压力、放量突破四个模板，以及短线、均衡、稳健三组参数风格，自动生成仍可编辑的安全 DSL 表达式；BRAIN 模式不要求用户预先知道窗口参数。桌面与手机验收通过，证据见 `docs/Plan/evidence/F7-factor-guided-input-web-2026-08-11/`。
