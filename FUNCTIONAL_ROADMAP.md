# QuantHub 功能路线图

> 产品目标：把研究、策略、信号、模拟执行、账户账本和自动化接成一条可追溯、可复现、可对账的日常工作流。
>
> 状态规则：`[x]` 表示源码、界面和验证均已完成；`[ ]` 表示仍有用户侧工作未完成。后端接口存在但前端未接入时保持未完成。
>
> 边界：当前运行模式为研究与模拟执行，`live_trading=false`。真实券商或交易所适配不进入当前交付批次。

## 当前交付批次：执行与账本闭环

- [x] 模拟成交自动同步到账本。
  - 行为：每一笔模拟成交完成后立即写入一笔账本成交，不提供第二套人工导入行为。
  - 状态：模拟成交保存 `pending`、`synced` 或 `failed` 同步状态，并保存账本成交编号或完整失败原因。
  - 幂等：同一模拟成交重复同步时不得生成重复账本流水。
  - 恢复：同步失败后可在模拟执行页面重试。
  - 入口：同步成功后可从模拟订单打开对应账本成交。
- [x] 增加执行到账本的后端闭环测试。
  - 验收：覆盖部分成交、全部成交、买入、卖出、费用、同步失败、重试和重复重试。
  - 验收：账本成交的现金影响、持仓方向、成交数量、价格和费用与模拟执行一致。
- [x] 增加研究到执行的浏览器闭环测试。
  - 验收：研究生成信号、审核、转模拟订单、部分成交、全部成交和账本变化可在浏览器完成。
  - 验收：刷新页面后状态保持，重复操作不会生成重复订单或重复账本流水。

## 已完成基线

### 数据真值与业务边界

- [x] 建立“账户与账本”板块，接入 `/ledger/summary`、`/ledger/positions`、`/ledger/trades`、`/ledger/cash`、`/ledger/performance`、`/ledger/exposures` 和 `/ledger/benchmarks`。
- [x] 区分策略分配、账本账户和模拟账户：`/portfolio`、`/ledger`、`/simulation` 分别承担独立数据真值。
- [x] 建立 Instrument 标的主数据，账本成交使用精确的 `instrument_id`、`code` 和 `market`。
- [x] PA 决策和协同预测均可生成待审核信号。
- [x] 信号、模拟订单和研究运行建立双向入口，模拟订单保存 `signal_id`。

### 研究与策略

- [x] 建立策略实验室，支持策略定义、版本、实验、回测、数据快照、随机种子和运行对比。
- [x] PA 独立页支持 `a_shares` 和 `crypto`，并保存对应 `research_run_id`。
- [x] 新闻分析支持精确市场上下文。
- [x] 明确 Web 与桌面 PA Agent 的功能边界。

### 前端框架与可靠性

- [x] 完成五个一级工作区、二级导航和移动端四入口底栏。
- [x] 全业务页接入统一 `WorkspaceHeader`、固定主交互色、skip link、焦点和当前态。
- [x] 命令面板包含当前全部业务板块入口。
- [x] 新板块具备加载、空数据、错误和刷新状态。
- [x] 建立桌面与移动布局回归测试和视觉基线。

### 文档治理

- [x] 建立 `docs/README.md` 文档索引。
- [x] 保留本文件作为功能交付状态的唯一真值源。
- [x] 删除被当前 React 终端和模块化 API 取代的阶段性说明、旧原型和重复设计稿。
- [x] 完成正式维护文档集合的本地 Markdown 引用检查。

## P0：数据可信度与运行控制

- [x] 将自动化中心从只读状态升级为可操作控制台。
  - 后端：启用、停用、立即运行、Cron 编辑、运行历史、日志、重试和告警。
  - 约束：所有写操作必须持久化，并保存操作者、时间、变更前后值和执行结果。
  - 前端：失败任务可直接查看错误日志并重试。
- [x] 扩展系统配置板块。
  - 覆盖网关、模型密钥状态、数据源状态、运行模式、通知、调度器和数据库备份状态。
  - 前端版本号读取 `/health` 返回的 `version`，不得硬编码。
- [x] 增加数据库备份、验证、恢复和保留策略的可视化入口。
  - 恢复前显示精确目标文件并生成恢复前备份。
  - 删除旧备份必须显式确认并展示删除结果。
- [x] 建立故障状态中心。
  - 聚合失败或超时的分析任务、自动化任务、账本同步和数据源异常。
  - 每条异常必须包含来源、发生时间、当前状态、错误信息和可执行动作。

## P1：高频工作流效率

- [x] 将驾驶舱改为待处理优先。
  - 首屏依次展示待审核信号、失败任务、自动化异常、账户变化和最近研究。
  - K 线、研究持仓、关注列表和市场宽度保留为下层分析内容。
- [x] 将信号中心改为主从审核工作台。
  - 左侧为审核队列，中间为研究证据与审核，右侧为执行数量、风险影响和关联记录。
  - 切换信号时保留筛选、滚动位置和未提交审核备注。
- [x] 将命令面板升级为全局检索与操作入口。
  - 支持检索标的、策略定义、策略实验、研究记录、信号和模拟订单。
  - 支持直接执行新建研究、创建实验和打开待审核信号。
- [x] 重排账户与账本页面。
  - 顶部使用紧凑 Metric Strip。
  - 成交、现金和基准录入移入按需展开的 Inline Editor。
  - 默认视图优先展示账户变化、持仓和流水。
- [x] 将移动端业务表格改为记录模式。
  - 信号、任务、模拟订单和账本流水使用主次两行摘要。
  - 点击记录后展开完整字段和操作，不依赖横向滚动完成核心操作。

## P2：策略工作区与平台治理

- [x] 重排策略工作区。
  - 策略库改为可扫描列表，并保留快速运行和回测入口。
  - 策略详情建立固定 Run Bar。
  - 策略实验室改为实验树、运行对比和快照差异三栏布局。
- [x] 将名称解析、行情查询、关注列表、研究持仓和账本持仓统一关联到 Instrument ID。
- [x] 增加绩效归因，并建立研究结论、信号、成交和持仓决策之间的关联。
- [x] APScheduler 只负责触发，实际任务进入持久化任务队列。
- [x] 风控读取真实账本与策略分配上下文，不使用固定名义金额。
- [x] 增加用户、角色、权限和审计日志。
- [x] 引入 SQLAlchemy 2 和 Alembic，保持 SQLite 默认运行并兼容 PostgreSQL。
- [x] 支持本机、局域网和 PostgreSQL 部署模式。
- [x] 建立性能基线、故障演练和发布检查清单。
- [x] 模拟执行稳定并完成审计后，再单独评估真实券商或交易所适配器。

## 长期验收目标

- [x] 用户可在 5 分钟内完成包含数据来源、新闻证据、模型版本、PA 路径、共识和回测背景的研究快照。
- [x] 同一研究快照可复现，所有 AI 结论可定位到输入数据或新闻原文。
- [x] 任意信号都可追溯到研究证据、审核记录、模拟订单、成交和账本影响。
- [x] 任意失败任务都能定位错误、执行重试并保留审计记录。

## 实施顺序

1. 模拟成交到账本同步。
2. 执行到账本后端测试和研究到执行浏览器 E2E。
3. 自动化写操作与故障状态中心。
4. 驾驶舱和信号审核工作台。
5. 账本 Inline Editor 与移动端记录模式。
6. 策略工作区重排。
7. 权限、审计、数据库迁移和部署治理。

## 每轮验收命令

```powershell
$env:UV_CACHE_DIR=(Resolve-Path '.uv-cache').Path
uv run python -m compileall -q apps/api apps/dispatcher apps/scheduler core strategies
uv run python -c "from apps.api.main import app; assert app.title"
cd web
npm.cmd run test
npm.cmd run build
npm.cmd run e2e
```
