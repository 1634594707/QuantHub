[build-system]
requires = ["hatchling>=1.21"]
build-backend = "hatchling.build"

[project]
name = "quanthub-apps-pa-agent"
version = "0.4.0"
description = "QuantHub 网页 PA 分析视图模型"
license = "AGPL-3.0-or-later"
requires-python = ">=3.11,<3.13"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["pa_agent"]
