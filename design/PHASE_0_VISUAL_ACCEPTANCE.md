# QuantHub Phase 0 视觉验收记录

> 基线日期：2026-07-26。截图来自本机 `http://localhost:5173`，后端 `/health` 返回 `status=ok`、`strategies=12`、`live_trading=false`。

## 基线范围

- [x] 驾驶舱 `/`
- [x] 标的研究 `/research/600519`
- [x] 信号中心 `/signals`
- [x] 账户与账本 `/ledger`
- [x] 策略实验室 `/strategy-lab`

每个页面均保存以下四种视口：

- [x] `1440×900`
- [x] `1024×768`
- [x] `768×1024`
- [x] `390×844`

截图目录：`design/baselines/2026-07-26/`，共 20 张 PNG。

## Phase 1 验收项

- [x] 桌面只显示驾驶舱、研究、策略、执行、运营五个一级工作区。
- [x] 当前工作区二级入口可见，原有 URL 保持不变。
- [x] 所有页面使用同一主交互色，涨跌与警告颜色不随页面变化。
- [x] 独立业务页使用 `WorkspaceHeader`，桌面高度不超过 72px。
- [x] 390px 显示驾驶舱、研究、信号、更多四入口底栏。
- [x] 390px、768px、1024px、1440px 无页面级横向溢出。
- [x] 键盘可进入 skip link，导航和按钮有明确焦点、当前态与按下反馈。
- [x] `prefers-reduced-motion` 继续关闭非必要动效。

## 质量基线

- [x] Phase 0 前端类型检查通过。
- [x] Phase 0 核心交互已完成历史验收。
- [x] Phase 0 生产构建通过。
- [x] Phase 1 类型检查和生产构建通过。

Phase 1 生产体积：JS 457.92 kB（gzip 143.48 kB），CSS 137.72 kB（gzip 23.72 kB）。与 Phase 0 相比，JS 减少 3.26 kB，CSS 增加约 4.4%，未超过 5% 门禁。

## 2026-07-27 前端细节验收

- [x] 驾驶舱、标的研究、信号中心、模拟执行、账户与账本、策略实验室完成四视口回归。
- [x] 截图目录 `design/baselines/2026-07-27/` 共 24 张 PNG，截图前等待各页面本地业务数据完成。
- [x] 仅图标操作具备可访问名称和悬停说明；skip link 在路由懒加载期间仍可获得并归还焦点。
- [x] 移动端信号审核、模拟成交、账本重试和记录展开在 `390×844` 完成操作验收。
- [x] 页面入口按路由拆包，本地字体不依赖外部网络请求。

最终质量结果：TypeScript 与生产构建通过；历史自动化测试套件已在网页项目精简时移除。

最终生产构建体积：JS 总计 587,502 bytes、CSS 总计 182,964 bytes；入口 JS 289,261 bytes（gzip 91.83 kB）、入口 CSS 52,988 bytes（gzip 10.28 kB）。总量超出 Phase 1 的 5% 上限，新增完整功能对应的构建块记录在 `design/FRONTEND_REDESIGN_PLAN.md`。
